/* eslint-disable react/react-in-jsx-scope */

import { Container } from './styles';

export function Vehicle(): JSX.Element {
  return (
    <Container>
      {/* Estou sem tempo para fazer o front, entao vou dar um resumo de como eu faria */}
      {/* nesse arquivo iria fazer um header com as informacoes como quantidade de carros nao vendidos e ouras infos */}
      {/* depois iria fazer uma listagem dos items da tabela vehicles aonde iria por uma paginacao tambem */}
      {/* depois teria um botao de add que iria para a pagina para adicionar um veiculo */}
      {/* teria na listagem o botao de deletar e outro de editar que iria para a pagina de editar */}
      {/* teria alguns filtros na listagem como é pedido no desafio, teria que alterar o back end para suportar esses filtros, nada muito dificil de fazer */}
      {/* no estilo eu instalei o tailwind, mas pode ser usado o styled-components que instalei tambem */}
    </Container>
  );
}
